import java.io.FileReader;
import java.io.IOException;

public class Main {
	
	private static String inputFileName = "", // Root of the input file.
				   outputFileNae = ""; // Root of the output file.
	
	public static void main(String[] args) {
		// Read the input file.
		readFile();
		
		
	}
	
	// Method that reads the file.
	private static void readFile() {
		// Try to read the file. In case it fails, the excception is caught.
		try {
			String line;
			String [] data;
			
			FileReader read = new FileReader(inputFileName);
			
			
		} catch(IOException e) {
			System.out.println("The file does not exist!");
		}
	}

}
